# This workflow has been replaced by the individual service-specific GitOps pipelines.
# Kept only for historical reference and manual fallback execution.

name: "Deploy — Build & Ship to EKS (Legacy)"

on:
  workflow_dispatch:
