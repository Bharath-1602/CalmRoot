apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.assessmentService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.assessmentService.name }}
spec:
  replicas: {{ .Values.assessmentService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.assessmentService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.assessmentService.name }}
    spec:
      serviceAccountName: {{ .Values.assessmentService.serviceAccount.name }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: {{ .Values.assessmentService.name }}
      containers:
        - name: {{ .Values.assessmentService.name }}
          image: "{{ .Values.global.image.registry }}/{{ .Values.assessmentService.image.repository }}:{{ .Values.assessmentService.image.tag | default .Values.global.image.tag | default "latest" }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.assessmentService.port }}
              name: http
          envFrom:
            - configMapRef:
                name: assessment-service-config
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.assessmentService.secrets.jwtSecretName }}
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.assessmentService.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.assessmentService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.assessmentService.resources | nindent 12 }}
