apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.therapistService.name }}
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.therapistService.name }}
  minReplicas: {{ .Values.therapistService.hpa.minReplicas }}
  maxReplicas: {{ .Values.therapistService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.therapistService.hpa.targetCPUUtilizationPercentage }}
